pipeline {
    agent any
    stages {
        stage("Test") {
            steps {
                sh "./gradlew clean test"
            }
        }
        stage("Publish") {
            steps {
                sh "./gradlew pactPublish"
            }
        }
    }
}